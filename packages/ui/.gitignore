node_modules/
*.log
.DS_Store

# TX-3: the bundled build (`npm run build`). src/ is the source of truth and stays committed; this
# is a generated artifact, like every other mirror in these repos, and it belongs in .gitignore
# beside them. `make copy-shared-ui` in each application builds it before vendoring it.
dist/
